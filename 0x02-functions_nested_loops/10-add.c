#include <stdio.h>
#include "holberton.h"
/**
 * add - adds two integers
 * @n: first argument of the function
 * @m: second argument of the function
 * Return: return the addition of the integers
 */

int add(int m, int n)
{
	int result;

	result = n + m;
	return (result);
}
