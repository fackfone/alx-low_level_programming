#include "holberton.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen_recursion - a function that retruns the length of a string
 * @s: first argument of the function and pointer
 *
 * Return: length of strig integer
 */

char _strlen_recursion(char *s)
{
	return (strlen(s));
}
