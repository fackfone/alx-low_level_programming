#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strcmp - function that compares two strings
 * @s1: first  argument of the function
 * @s2: second argument of the function
 * Return: 0 if s1 is found in s2
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
